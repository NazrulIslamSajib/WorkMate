import React from 'react';

const Layout = ({ children }) => {
  return (
    <div className="app-layout">
      {/* TODO: Add header, sidebar, and other layout elements based on Figma */}
      {children}
    </div>
  );
};

export default Layout;
